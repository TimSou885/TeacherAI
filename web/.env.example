# Copy to .env and fill in values
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJxxxxxx
# Dev: 留空會自動用 http://localhost:8787（請先在 api 目錄執行 npm run dev）
# 若用 proxy（不設 VITE_API_URL 且改為同源）曾出現 401，可改設 VITE_API_URL=http://localhost:8787
# Prod: 填 Workers 網址（例：https://teacherai.xxx.workers.dev）
VITE_API_URL=
